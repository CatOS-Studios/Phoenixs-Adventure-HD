const maxHealth = 100;
maxHealth = 50; // Not allowed, will throw an error
currentHealth = maxHealth
//maxHealth = currentHealth;
var numberOfLives = 3;

  var isGameOver= false;
isGameOver = true;
  var isAtEnd = false;
  var characterName = "Me";
characterName = "Phoenix"
  var age = 13;

let message = `Hi, Welcome to ${characterName}'s world. His age is ${age}`

let length = characterName.length;
let upperName = characterName.toUpperCase();
characterName = characterName.toLowerCase();

let p = characterName.slice(0,6);

var level = 1
var title - `Level ${level}`;

var level = 2
var title - `Level ${level}`;

var level = 3
var title - `Level ${level}`;

var level = 4
var title - `Level ${level}`;

var level = 5
var title - `Level ${level}`;

health = health + 10; // 60
health = health % 50; // 10
// health % 2 == 0
health = health ** 2 // 100
health -= 20; // health = health - 20
